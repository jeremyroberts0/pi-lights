# Commands used by the gibon autonomous coding agent to validate work during coding sessions
dependencies: npm ci  # Install exact dependencies from package-lock.json
lint: npm run test:lint  # Run ESLint with auto-fix
unit: npm test  # Run all tests (currently just linting)
build: npm run build:prod  # Install production dependencies only